let awnserResults = "no";

function setup() {
    createCanvas(500, 700);

}

function draw() {
    background(220);

    fill(0);
    ellipse(width/2, height/2, 300, 300);

    fill(255, 0, 0);
    textAlign(CENTER, CENTER);
    textSize(32);
    text("8", width/2, 300);
}
